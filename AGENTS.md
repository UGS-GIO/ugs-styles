# ugs-styles (agent context)

Repo-specific conventions. Org-wide rules live in the shared `agents-config` AGENTS.md — this
file only covers what's specific to this repo.

## Code comments

- Almost none. One line, tops — aim for zero.
- Never reference a Jira ticket, GitHub PR, or issue number.
- A style file is `itemId`, `archetype`, `field`/`color`, `palette`. That's self-explanatory;
  don't narrate it.
