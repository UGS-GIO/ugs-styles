{
  "name": "@ugs-gio/styles",
  "version": "0.1.0",
  "description": "MapLibre style modules for UGS map viewers. Source-of-truth for vector styling. Build outputs CDN-hosted JSON consumed via STAC renders extension.",
  "private": true,
  "type": "module",
  "scripts": {
    "build:json": "tsx scripts/build-json.ts",
    "watch": "tsx watch scripts/build-json.ts",
    "coverage": "tsx scripts/coverage.ts",
    "typecheck": "tsc --noEmit",
    "qa": "npm run build:json && npx --yes http-server -p 8080 -d false -c-1"
  },
  "peerDependencies": {
    "maplibre-gl": ">=4"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "maplibre-gl": "^5.23.0",
    "tsx": "^4.21.0",
    "typescript": "^6.0.3"
  }
}
